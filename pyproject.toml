[project]
name = "charruallm"
version = "0.1.0"
description = "Tiny LLM base model trained on Uruguayan Spanish news"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.4",
    "numpy>=1.26",
    "tokenizers>=0.20",
    "tqdm>=4.66",
]

[tool.uv]
package = false
